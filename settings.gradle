pluginManagement {
    repositories {
        maven {
            url = 'https://maven.minecraftforge.net/'
            name = 'forge'
            mavenContent { releasesOnly() }
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'gitpatcher'
